[package]
name = "abc349"
version = "0.1.0"
edition = "2021"

# dependencies added to new project
[dependencies]
proconio = "*"
superslice = "*"
itertools = "*"
regex = "*"

[profile.release]
lto = true
panic = 'abort'
